
export let adminInitialState = {
    linksList: [
        '/', '/dota2', '/lol', '/hots', '/adminPanel'
    ]
}

const adminPanelFormsReducer = (state = adminInitialState, action) => {

            return state;
}

export default adminPanelFormsReducer